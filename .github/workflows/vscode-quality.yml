name: VS Code Extension Quality

on:
  pull_request:
  workflow_dispatch:

jobs:
  vscode-quality:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: vscode-trivy-ls
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: vscode-trivy-ls/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Check formatting
        run: npm run format:check

      - name: Check types and build
        run: npm run lint
